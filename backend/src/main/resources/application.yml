spring:
  sql:
    init:
      mode: always
      separator: ';;'
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/hanchengzhe}
    username: ${SPRING_DATASOURCE_USERNAME:hanchengzhe}
    password: ${SPRING_DATASOURCE_PASSWORD:810918zhuzhu}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}
mybatis:
  mapper-locations: classpath:/mappers/*.xml
  type-aliases-package: com.example.blogx.post,com.example.blogx.user,com.example.blogx.comment
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 8080
  servlet:
    context-path: /
springdoc:
  api-docs:
    enabled: false
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}
