<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blogx.user.UserTokenMapper">

    <resultMap id="UserTokenResultMap" type="com.example.blogx.user.UserTokenEntity">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="refreshToken" column="refresh_token" />
        <result property="expiresAt" column="expires_at" jdbcType="TIMESTAMP" />
        <result property="createdAt" column="created_at" jdbcType="TIMESTAMP" />
    </resultMap>

    <insert id="insert" parameterType="com.example.blogx.user.UserTokenEntity">
        INSERT INTO user_tokens (id, user_id, refresh_token, expires_at, created_at)
        VALUES (
            #{token.id},
            #{token.userId},
            #{token.refreshToken},
            #{token.expiresAt, jdbcType=TIMESTAMP},
            #{token.createdAt, jdbcType=TIMESTAMP}
        )
    </insert>

    <select id="findByRefreshToken" parameterType="string" resultMap="UserTokenResultMap">
        SELECT
            id,
            user_id,
            refresh_token,
            expires_at,
            created_at
        FROM user_tokens
        WHERE refresh_token = #{refreshToken}
        LIMIT 1
    </select>

    <delete id="deleteByRefreshToken" parameterType="string">
        DELETE FROM user_tokens WHERE refresh_token = #{refreshToken}
    </delete>
</mapper>
